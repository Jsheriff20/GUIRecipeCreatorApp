#pragma once
#include <iostream>
#include <string>

using namespace std;
class SearchForIngredient
{
public:
	SearchForIngredient();
	~SearchForIngredient();
	bool findIngredient(string ingredient);
};

